<apex:page standardController="In_Kind_Donation_Reimbursement__c" tabStyle="In_Kind_Donation_Reimbursement__c" >
 
 <style>
 #background{
    position:absolute;
    z-index:0;
    background:white;
    display:block;
    min-height:100%; 
    min-width:100%;
    color:yellow;
}

#bg-text
{
    color:lightgrey;
    font-size:25px;
    transform:rotate(300deg);
    -webkit-transform:rotate(0deg);
    -webkit-user-select: none;  
    -moz-user-select: none;     
    -ms-user-select: none;     
    user-select: none;           
}
 </style>
 <div id="background">
  <apex:outputPanel rendered="{!In_Kind_Donation_Reimbursement__c.Documentation__c =='Will Attach' }">
   <br/><p id="bg-text">You Can Submit this record for approval by clicking Submit for approval button on top of the page</p>

  </apex:outputPanel>
  
   <apex:outputPanel rendered="{!OR(In_Kind_Donation_Reimbursement__c.Documentation__c =='Not available', In_Kind_Donation_Reimbursement__c.Documentation__c =='')}">
  <br/><p id="bg-text">Please Attach all in-kind related receipts or documentation below or select Attachments No-Not available above. Form 8283 can also be attached if provided by donor.</p>

  </apex:outputPanel>
 
  </div>
 </apex:page>